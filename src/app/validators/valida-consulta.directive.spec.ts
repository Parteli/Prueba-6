import { ValidaConsultaDirective } from './valida-consulta.directive';

describe('ValidaConsultaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaConsultaDirective();
    expect(directive).toBeTruthy();
  });
});
